# RETO 13M — Release 2026-09-20

Proyecto: **«Remix of Reto 13M»** `d243f279-2db6-4f18-a269-029cf284267f`
Base: **`wpiztubmmmzclhlprgpd`** · Rama: `claude/eager-noether-s7p33g` · 11 commits

---

## AVISO IMPORTANTE SOBRE QUE SIGNIFICA "RELEASE" AQUI

**El backend YA ESTA EN PRODUCCION.** Cada migracion de esta sesion se aplico
sobre la base viva conforme se verificaba. No hay un "deploy" pendiente del
backend: lo que este documento describe es lo que **ya esta sirviendo ahora**.

Lo que NO esta hecho: **la revision visual del frontend**. Sigue bloqueada por el
login y **no puedo firmarla**. Esto es un release de backend verificado con un
frontend sin verificar visualmente. Decirlo de otra forma seria mentir.

---

## 1. Estado verificado en produccion

| superficie | filas | con analisis | O/U | dinero |
|---|---|---|---|---|
| FUTBOL · `v_futpro_publication_v3` | 36 | 36 | **0** | **0** |
| FUTBOL · `v_tarjeta_soccer_v1` | 36 | 36 | **0** | **0** |
| MLB · `v_mlb_publication_v1` | 33 | 33 | — | **0** |
| NFL · timeline Elo (legible por `anon`) | 97 | 97 | — | **0** |
| TODOS · `v_pick_canonico` | 202 | — | — | **0 con dinero** |

**Guardias: 6 de 6 en PASS.**

```
G37.1 la tarjeta publica no esta vacia        PASS
G37.2 la cache no esta rancia                 PASS
G37.3 el calculo y lo servido coinciden       PASS
G50.1 candidatos sin promover                 PASS
G50.2 modelo promovido sin predicciones       PASS
G50.3 deporte declarado sin cerebro           PASS
```

---

## 2. Lo que cambia para el usuario

### NFL — de no existir a ganarle a adivinar

| | antes | despues |
|---|---|---|
| partidos evaluados | 16 | **516** |
| acierto | 56.3% | **64.2%** |
| upper95 | +0.10948 | **−0.01225** |
| veredicto | no se sabe | **LE GANA A ADIVINAR** |

Habia **1,725 partidos desde 2021** sin usar. El laboratorio Elo corria por cron a
diario y **la promocion no tenia cron: nadie cosechaba**. Ademas la captura de
futuros excluia NFL por una lista escrita a mano, con 255 partidos por delante
contra 1 de NBA. Los tres candados, abiertos.

### FUTBOL — deja de afirmar lo que no puede probar

- La insignia verde **"Validado con resultados reales"** desaparecio de 102
  tarjetas. La ganaba el prior (`crossleague_v1`), no el modelo que publica.
- **Over/Under fuera del API en sus tres formas**: columnas, `mejor_pick` (que
  nombraba un total en 49 de 97 filas) y un jsonb con `over25/35/45` escondido.
- El **linaje del prior** por fin se ve, separado del veredicto, con su
  advertencia de que la evidencia no se hereda.
- **Cobertura intacta**: no se retiro un partido, se retiro una afirmacion.

### MLB

Consulta rota arreglada (pedia una columna inexistente y tiraba toda la
respuesta). Le gana a adivinar. Los "21 pp de mala calibracion" resultaron ser
**7 partidos de ruido**.

---

## 3. Lo que se probo y se RECHAZO

Cuatro experimentos. **Tres rechazados por su propia prueba.**

| experimento | veredicto | por que |
|---|---|---|
| Calibrador por compresion (NFL) | RECHAZADO | la ventana de ajuste elige "no calibrar" |
| Calibrador Platt (NFL) | RECHAZADO | gana 0.00003 (ruido) y empeora la validacion |
| Elo con margen de victoria | RECHAZADO | gana en train por ruido, **pierde en holdout** |
| Promocion del Elo de NFL | **ACEPTADO** | 516 partidos, upper95 −0.012 |

En los tres rechazos existia una configuracion que **le ganaba al modelo vivo en
el holdout**. Tomarla habria sido seleccionar sobre el conjunto de validacion. No
se hizo ninguna vez.

---

## 4. Lo que NO esta verificado — leelo antes de presumir el release

| | estado |
|---|---|
| Revision visual del Remix (movil y escritorio) | **NO HECHA** — bloqueada por login |
| `anon` por HTTP real contra PostgREST | **NO EJECUTADA** — el proxy de salida deniega el CONNECT. Si se probo `SET ROLE anon` dentro de la base |
| Calibracion de cualquier modelo | **NO ESTABLECIDA** en ningun deporte |
| Picks con dinero | **CERO, a proposito** |

El frontend fue publicado por otro agente (commit `ad30e27`, audiencia publica)
**sin revision visual**. Eso no lo firmo yo.

---

## 5. Deudas conocidas, declaradas y NO arregladas

- `pa_audit_log` (302,168 filas) escribible por `authenticated`
- `respaldo_borrado_usuarios` y `usuarios_archivados` legibles por `anon`
- 4 fugas de lectura con `USING(true)`
- `registrar_perfil` deja que el cliente elija su `bankroll_inicial` ← **la peor si algun dia se enciende el dinero**
- recursion infinita en `usuarios_auth_insert`
- `v_picks_futbol_calc`: superficie muerta con vocabulario de EV y Kelly
- `refresh_selected_team_elo_models()` sigue sin cron (decision de producto)

---

## 6. Rollback

Cada cambio tiene el suyo, documentado:

| cambio | rollback |
|---|---|
| ISS257 veredicto | `shadow-patches/iss257/rollback_v_evidencia_mercado_soccer.sql` |
| ISS258 O/U fuera del API | `shadow-patches/iss258/rollback_ou_api_publica.sql` (**probado en BEGIN/ROLLBACK**) |
| ISS260 picks futbol calc | `shadow-patches/iss260/rollback_picks_futbol_calc.sql` |
| ISS262/263 NFL | `docs/ISS262_nfl_tenia_cinco_temporadas_y_ningun_modelo.md` |
| ISS264 guardia | `drop function public.gate_cerebro_sin_cosechar()` |
| ISS265 laboratorio | `drop function v2.fn_team_elo_backtest_mov(...)` |

**Ningun cambio borro datos historicos.**

---

## 7. La frase honesta del release

La app **no te miente en ningun sitio que yo haya podido medir**, y por primera
vez hay un deporte con habilidad demostrada sobre cientos de partidos.

**Todavia no te hace ganar dinero, y no lo pretende.** Cero picks monetizables en
los tres deportes, porque la calibracion se probo y se rechazo con evidencia.
Lo que falta es temporada jugada, y eso no lo acelera el codigo.
