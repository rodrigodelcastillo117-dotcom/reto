# App legada de Streamlit (reto.py)
streamlit>=1.32
gspread>=5.12
oauth2client>=4.1.3
pandas>=2.0
plotly>=5.20

# Declaradas ahora, no antes: el codigo YA las importaba y un clon limpio no
# podia ejecutar nada. requests y pytz los usa reto.py (su propio docstring ya
# nombraba requests); numpy y scipy los usan los ajustes de lab/
# (fit_crossleague.py, validate_v2.py, validate_domestic.py).
requests>=2.31
pytz>=2024.1
numpy>=1.26
scipy>=1.11
